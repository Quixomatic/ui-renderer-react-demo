/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './src/**/*.{js,jsx,ts,tsx}',
  ],
  // In v4, most theme configuration is done via CSS
  // We'll keep custom colors and extend configuration minimal
}