/**
 * Checks if value is null or undefined
 * @param input - input parameter
 * @returns {boolean} value true if the input is null or undefined, else returns false.
 */
export default function isNil(input) {
	return input == null;
}
