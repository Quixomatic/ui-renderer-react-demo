/**
 * Checks if value is classified as an `Array` object
 * @param {*} input Input array parameter
 * @returns Returns boolean true or false.
 */
export default function isArray(input) {
	return Array.isArray(input);
}
