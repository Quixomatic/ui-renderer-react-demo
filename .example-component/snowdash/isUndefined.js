/**
 * The function checks if value is undefined.
 * @param input - The input parameter.
 * @returns returns a boolean true or false whether the input is undefined.
 */
export default function isUndefined(input) {
	return typeof input === 'undefined';
}
